[project]
name = "aiecho-react-app"
version = "0.1.0"
description = "Echo app with FastAPI backend and React frontend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
]

[project.scripts]
server = "main:main"
